# AIMO에서 다수결 투표(Voting)와 토론(Debate)이 정확도를 높이는 물리적/수학적 근거

LLM이 수학 문제를 풀 때 단일 시도(Zero-shot)보다 다수결 투표(Self-Consistency)와 다중 에이전트 토론(Multi-Agent Debate)을 사용할 때 성능이 비약적으로 상승하는 데에는 명확한 통계적, 구조적 이유가 있습니다.

## 1. 다수결 투표(Self-Consistency/Majority Voting)의 근거

이 원리는 2022년 Google Brain의 논문 *"Self-Consistency Improves Chain of Thought Reasoning in Language Models"* 에서 증명된 개념을 바탕으로 합니다.

### A. 패스 앳 케이 (Pass@k) 확률 모델
LLM은 생성 모델이기 때문에, 수학 문제를 풀 때 정답 경로를 밟을 확률($p$)과 오답 경로로 빠질 확률($1-p$)을 가집니다.
단일 프롬프팅에서는 정답을 맞출 확률이 단순히 $p$입니다.
하지만 $k$번 독립적으로 생성(Temperature > 0)하여 하나라도 정답이 포함될 확률(Pass@k)은 $1 - (1-p)^k$로 급격히 상승합니다.

AIMO와 같은 단답식 주관식 기출문제에서는 **"서로 다른 잘못된 논리가 우연히 정확히 같은 오답 수치로 수렴할 확률은 극히 0에 가깝다"** 는 수학적 특성을 이용합니다.
*   **정답의 수렴성**: 정답으로 가는 길은 여러 개(대수적 풀이, 기하학적 풀이 등)일 수 있지만, 그 목적지(답)는 하나입니다.
*   **오답의 발산성**: 계산 실수나 논리적 오류를 범할 경우, 그 결과값은 랜덤하게 흩어집니다(발산).

결과적으로 10번을 생성했을 때, 정답률 $p$가 30%밖에 안 되는 모델이라도:
*   정답(예: 42)은 3번 등장합니다.
*   나머지 7번의 오답은 [15, -3, 102, 5, 42.5, 9, 0] 처럼 각기 다른 숫자로 흩어집니다.
*   **다수결 원칙(가장 빈도가 높은 답 선택)을 적용하면, 오답들의 빈도는 보통 1이기 때문에 빈도수가 3인 정답 '42'가 최종 답으로 채택됩니다.** 이것이 정확도가 올라가는 결정적인 수학적 근거입니다.

## 2. 토론(Multi-Agent Debate)의 근거

이 원리는 *"ChatEval"* 또는 *"Socratic Models"* 와 같은 연구에서 입증된 'LLM의 자기 교정(Self-Correction)' 능력을 바탕으로 합니다.

### A. 생성(Generation)과 검증(Verification)의 비대칭성
인간과 마찬가지로 LLM도 **"어려운 문제를 처음부터 끝까지 실수 없이 푸는 것(Generation)"**보다 **"남이 풀어놓은 풀이 과정에서 논리적 헛점이나 계산 실수를 찾아내는 것(Verification)"**에 훨씬 적은 인지적(연산적) 부하가 듭니다.

*   **Coder의 한계**: 코드를 처음 작성할 때는 '문제 해결 로직 구성' + '문법적 오류 없는 코드 작성' + '제약조건 암기'를 동시에 수행해야 하므로 Context window 내에서 주의력(Attention) 분산이 일어납니다 (환각 발생).
*   **Reviewer의 강점**: 이미 작성된 코드와 실행 결과를 볼 때는 '이 식이 전제 조건(예: $x > 0$)을 위반하지 않았는가?'라는 단일 목적에만 Attention을 집중할 수 있어, Coder가 놓친 코너 케이스나 $N=0$ 일 때의 예외 등을 예리하게 잡아냅니다.

### B. 확증 편향(Confirmation Bias) 파괴
LLM은 Auto-regressive(자기 회귀) 모델입니다. 즉, 자신이 방금 뱉은 단어를 기반으로 다음 단어를 예측합니다. 만약 첫 번째 줄에서 방향을 잘못 잡았다면, 모델은 자신이 뱉은 오답 토큰에 조건부 확률이 묶여 끝까지 오답을 정당화하려는 경향(확증 편향)을 보입니다.
*   역할을 분리한 에이전트(Reviewer)는 이전 에이전트의 내부 Hidden state(은닉 상태) 확률에 종속되지 않은 백지상태의 새로운 프롬프트에서 시작하므로, 첫 단추가 잘못 끼워진 것을 객관적으로 지적할 수 있습니다.

## 요약

*   **다수결 투표가 작동하는 이유**: 오답은 무한히 다양한 수치로 흩어지지만(발산), 정답은 하나의 수치로 뭉치기(수렴) 때문에, 빈도수 1위를 뽑으면 정답일 확률이 극적으로 높아집니다.
*   **토론이 작동하는 이유**: LLM은 정답을 '생성'하는 것보다 논리적 오류를 '검증'하는 데 훨씬 뛰어난 구조적 특성(Attention 집중도)을 가지며, 에이전트 분리를 통해 LLM 특유의 자가당착(확증 편향)을 끊어낼 수 있기 때문입니다.
